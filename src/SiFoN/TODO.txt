#!/usr/bin/env python
# coding: utf-8

# ### Retrieving SNPs

# TODO

### Smoothing 

# TODO: vmin, vmax

#### TODO: we can probably simplify the abs value code by doing the exact same steps, but take the abs value at the very end. this will also confirm whetehr o not the signed function is working correctly. both approaches should produce the same data/graphs/

# can also add single sequence classes or all the sequence classes instead of just the max.

# add kaeledo to requirements.txt

# Add module docstrings
